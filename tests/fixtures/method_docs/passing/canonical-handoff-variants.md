<!-- method-validate-docs: fixture -->
# Canonical handoffs

The `discover` stage begins immediately after `kickstart`.
The `plan` stage begins only after accepted Discovery and PRD gates.
Never hand off directly from `kickstart` to `plan`.
