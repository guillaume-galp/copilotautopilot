<!-- method-validate-docs: fixture -->
# Legacy handoff variants

Immediately after `kickstart`, the `plan` stage begins.
`kickstart` is immediately followed by `plan`.
The `plan` stage directly follows `kickstart`.
